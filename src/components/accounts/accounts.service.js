'use strict';

/**
 * Service for manipulating accounts
 */
 angular.module( 'docsx.accounts', [] ).service( ['$q', 'googleApi', 'clientId', 'scope', '$http'
 	,function( $q, googleApi, clientId, scope, $http ){
 		
 	}
 ] );